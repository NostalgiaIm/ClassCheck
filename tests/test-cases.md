# CatCheck（喵喵查寝）测试用例

执行日期：2026-09-19
测试环境：Windows 11、Chromium 系浏览器、`http://127.0.0.1:5174/`（若端口被占用，以 Vite 输出的本地端口为准）、本地开发服务器

| 编号 | 模块 | 前置条件 | 操作 | 预期结果 | 结果 |
| --- | --- | --- | --- | --- | --- |
| TC-001 | 启动 | 浏览器支持 IndexedDB | 打开应用 | 首页可打开，显示“仅本机”，无网络依赖报错 | 待执行 |
| TC-002 | PWA | 启动开发服务器 | 查看应用清单并刷新页面 | 可读取 `manifest.webmanifest`，Service Worker 注册成功 | 待执行 |
| TC-003 | CSV 导入 | 打开名单管理 | 选择 `fixtures/学生名单_正常.csv` | 出现导入预览，识别 4 条学生记录 | 待执行 |
| TC-004 | 文本导入 | 打开名单管理 | 选择 `fixtures/学生名单_文本.txt` | 能按标题识别宿舍号、姓名、班级 | 待执行 |
| TC-005 | Markdown 导入 | 打开名单管理 | 选择 `fixtures/学生名单_异常与重复.md` | 重复记录被去重，缺少必要字段的行显示错误 | 待执行 |
| TC-006 | Excel 导入 | 准备含多个工作表的 `.xlsx` | 选择名单文件 | 多工作表合并预览，宿舍号排序正常 | 待执行 |
| TC-007 | 图片识别 | 打开名单管理 | 选择 `fixtures/学生名单_图片.png`，等待 OCR 完成 | 使用本地 OCR 识别文本，显示可确认表格，不上传图片 | 待执行 |
| TC-008 | 取消预览 | 已出现导入预览 | 点击“取消” | 预览清除，学生名单数量不变 | 待执行 |
| TC-009 | 确认导入 | 已出现有效预览 | 点击“确认导入” | 学生写入本机，宿舍列表出现，刷新后数据仍在 | 待执行 |
| TC-010 | 空文件 | 准备空 TXT/CSV | 选择文件 | 显示没有可导入记录，不写入空数据 | 待执行 |
| TC-011 | 超大文件 | 准备超过限制的名单文件 | 选择文件 | 显示大小限制提示，页面保持可操作 | 待执行 |
| TC-012 | 公式注入拦截 | 文件中含 `=HYPERLINK(...)` 等值 | 选择文件 | 疑似公式行被跳过并提示，不进入名单 | 待执行 |
| TC-013 | 名单去重 | 文件含相同宿舍、姓名、班级记录 | 选择文件并确认 | 只保留一条，不产生重复学生卡片 | 待执行 |
| TC-014 | 按宿舍点名 | 已导入名单 | 首页选择宿舍 | 进入对应宿舍，不进入班级混合点名页面 | 待执行 |
| TC-015 | 一键全到 | 已进入宿舍点名 | 点击“一键全到” | 所有学生状态变为“到”，异常数量归零 | 待执行 |
| TC-016 | 修改异常 | 已一键全到 | 将两名学生改为“未到”和“请假”，添加备注 | 状态、颜色和统计即时更新，备注不丢失 | 待执行 |
| TC-017 | 保存查寝 | 已修改异常 | 点击“保存并生成文字” | 保存成功，生成规定格式的汇报文字 | 待执行 |
| TC-018 | 汇报文字 | 已保存查寝，查寝人为“查寝员甲” | 查看汇报区域 | 包含 `X月X日Y寝（查寝员甲）`、应到/实到/请假和班级请假名单 | 待执行 |
| TC-019 | 历史记录 | 已保存至少一条查寝 | 打开历史并按日期、宿舍筛选 | 筛选准确，可重新编辑，汇报文字一致 | 待执行 |
| TC-020 | 汇报文字复制 | 历史详情已打开 | 点击“复制” | 剪贴板包含完整文字 | 待执行 |
| TC-021 | 汇报导出 | 当前或历史详情已打开 | 分别点击文字、PNG、JPG、Excel | 浏览器下载或指定目录出现对应文件，内容一致 | 待执行 |
| TC-022 | 名单多格式导出 | 已导入名单 | 分别导出 Excel、CSV、Markdown、JSON | 生成 4 种文件，字段为宿舍号、姓名、班级 | 待执行 |
| TC-023 | 目录选择 | Chromium 且允许目录访问 | 选择存档目录后导出名单 | 文件写入指定目录，页面显示目录名称 | 待执行 |
| TC-024 | 目录回退 | 浏览器不支持目录访问或拒绝权限 | 导出文件 | 自动回退浏览器下载，仍能取得文件 | 待执行 |
| TC-025 | JSON 备份 | 已有名单和历史 | 导出备份 JSON | 包含 students、sessions、records，文件可读取 | 待执行 |
| TC-026 | 备份恢复预览 | 清空或更换浏览器站点数据 | 选择备份 JSON | 先显示待恢复数量，未确认前不写库 | 待执行 |
| TC-027 | 备份恢复 | 已出现恢复预览 | 点击“确认恢复” | 名单和历史合并恢复，查寝人名称恢复 | 待执行 |
| TC-028 | 恶意 HTML | 名单中含 `<img onerror=...>` | 导入并确认 | 页面只显示文本，不执行 HTML 或脚本 | 待执行 |
| TC-029 | 备注边界 | 输入超过 80 字或含控制字符 | 保存查寝 | 输入受限或清理，不破坏布局和历史记录 | 待执行 |
| TC-030 | 本地离线 | 关闭网络或拦截外部请求 | 重新打开并完成导入/点名/导出 | 核心流程仍可用，无外部 CDN 请求 | 待执行 |
| TC-031 | 数据整洁 | 导入空格、全角符号、重复行、缺列行 | 查看预览和名单 | 文本归一化，重复和错误有明确提示 | 待执行 |
| TC-032 | 删除宿舍 | 已有宿舍与历史 | 删除宿舍并确认 | 关联学生、查寝批次和明细一并删除 | 待执行 |
| TC-033 | 开始查寝 | 已导入名单 | 在“今日”点击“开始查寝”，输入查寝人姓名并确认 | 进入“选择宿舍”二级页面，显示本次查寝人和进度 | 待执行 |
| TC-034 | 查寝顺序 | 已进入“选择宿舍” | 点击宿舍卡片，保存后点击“下一个寝室” | 进入三级点名页，保存后跳转下一个未完成宿舍 | 待执行 |
| TC-035 | 异常原因 | 已进入三级点名页 | 默认全员为“到”，再改为“未到”和“请假” | “到”不显示原因栏，异常状态分别显示原因输入栏 | 待执行 |
| TC-036 | 结束与导出 | 已完成至少一个宿舍 | 点击“结束查寝”，在导出页选择文字、PNG、JPG、Excel | 进入导出页，预览包含本次查寝内容，可分别生成对应格式 | 待执行 |
| TC-037 | 底部安全区 | 手机或窄屏浏览器 | 查看底部导航和三级页面操作栏 | 控件位于系统导航区域上方，保存与下一个寝室按 1:3 比例排列 | 待执行 |
| TC-038 | 品牌与图标 | 安装 APK 或 PWA | 查看桌面名称、应用顶部名称和图标 | 默认显示“喵喵查寝”，可见 CatCheck 标识，猫咪图标外部白色背景透明 | 待执行 |
| TC-039 | 查寝人弹窗位置 | 已导入名单 | 点击“开始查寝” | 姓名输入窗口位于可用屏幕正中央，不贴底部导航栏 | 待执行 |
| TC-040 | Android 导航避让 | Android 手势/三键导航设备 | 打开“今日”“历史”“名单”页面并切换 | 底部工作栏固定在系统导航栏上方，不被按键覆盖或随切换跳动 | 待执行 |
| TC-041 | 固定双层顶栏导航 | 手机或窄屏浏览器 | 上下滚动“今日”“历史”“名单”任一长页面 | 顶栏不滚动；首层左侧图标、中央“喵喵查寝”，第二层三等分显示“今日、历史、名单” | 待执行 |
| TC-042 | 走读标记 | 已导入名单 | 在“名单”中点击宿舍卡片第三个“走读”按钮，按姓名点击标记 | 进入走读名单页；标记可保存、返回名单后显示走读人数 | 待执行 |
| TC-043 | 走读不影响汇报 | 已标记一名走读学生 | 完成该学生所在宿舍查寝并查看导出预览 | 应到/实到/请假统计与班级请假人文字仅由查寝状态决定，不出现走读字段 | 待执行 |
| TC-044 | 状态加固与输入稳定 | Android 设备，已导入名单 | 在“开始查寝”姓名框输入文字并弹出键盘；在点名页切换状态、滚动至最后一名学生并保存 | 弹窗保持居中不飞到顶部；保存栏紧跟最后一名学生；非法状态/缺失宿舍/空查寝结束均回退至安全页面并给出提示 | 待执行 |
